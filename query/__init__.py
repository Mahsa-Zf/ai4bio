"""Query utilities for semantic search over the Qdrant sentence index.

The package exposes the search workflow used to embed a natural-language query,
retrieve the closest sentence matches from Qdrant, and rehydrate sentence
metadata from MySQL for display.
"""
